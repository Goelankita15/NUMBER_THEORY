Divisors occurs in pairs
If we found a such that n % a == 0 then n/a = b is also a divisor of n 
(a, b) -> one should be less than sqrt(n) and other above sqrt(n)

if(n == 1) return 0;
for(int i = 2; i*i <= n; i++){
    if(n % i == 0) return 0;
}
return 1;

O[sqrt(n)]
