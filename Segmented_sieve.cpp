L R -> difference sholud be  10^6

generate primes till sqrt(R)
create dummy array of R,- L + 1 size
mark all multiples of prime till sqrt(R) not prime


Factorisation using sieve
set all numbers as primes
for each prime check if -1 is present then place a[j] = i;
